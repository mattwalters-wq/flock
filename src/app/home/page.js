'use client';
import { MarketingHome } from '@/components/MarketingHome';

// Stable, host-independent URL for the marketing homepage (also rendered
// automatically on the apex domain by app/page.js). Useful for previews.
export default function HomePage() {
  return <MarketingHome />;
}
